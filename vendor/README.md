# vendor

- `three.min.js` — Three.js r158 UMD build (MIT, © 2010-2023 Three.js Authors), taken from the `three@0.158.0` npm package (`build/three.min.js`). The leading deprecation `console.warn` was replaced with a no-op expression; nothing else was changed.
